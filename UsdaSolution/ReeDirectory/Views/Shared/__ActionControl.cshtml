@using ReeDirectory.EncyDyc

@model ReeDirectory.EntityFM.Entities.EBase

@if (((ReeDirectory.EntityFM.ExternalEntity.ESecurity)ViewBag.Security).Edit > 0)
{
    @Html.EncodedActionLink("Edit", "Edit", new { id = Model.Id }, null)
}

@if (((ReeDirectory.EntityFM.ExternalEntity.ESecurity)ViewBag.Security).Print > 0)
{
    @Html.EncodedActionLink("Details", "Details", new { id = Model.Id }, null)
}

@if (((ReeDirectory.EntityFM.ExternalEntity.ESecurity)ViewBag.Security).Delete > 0)
{
    @Html.EncodedActionLink("Delete", "Delete", new { id = Model.Id }, null)
}