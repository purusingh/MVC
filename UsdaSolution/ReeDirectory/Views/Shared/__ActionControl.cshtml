@using ReeDirectoryUtility
@using ReeDirectory.ReeHtmlHelper


@model ReeDirectoryEntityFm.Entities.Base.EBase

@if (((ReeDirectoryEntityFm.ExternalEntity.ESecurity)ViewBag.Security).Edit > 0)
{
    @Html.EncodedActionLink("Edit", "Edit", new { id = Model.Id }, null)
}

@if (((ReeDirectoryEntityFm.ExternalEntity.ESecurity)ViewBag.Security).Print > 0)
{
    @Html.EncodedActionLink("Print", "Print", new { id = Model.Id }, null)
}

@if (((ReeDirectoryEntityFm.ExternalEntity.ESecurity)ViewBag.Security).Delete > 0)
{
    @Html.EncodedActionLink("Delete", "Delete", new { id = Model.Id }, null)
}